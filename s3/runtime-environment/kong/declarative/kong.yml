_format_version: "3.0"
_transform: true

services:
- name: httpbin_service
  host: httpbin.org
  port: 80
  protocol: http
  routes:
    - name: debug_route
      paths:
        - /debug
      strip_path: true
  #
  # Configure token validation here
  # See : https://konghq.com/blog/engineering/jwt-kong-gateway
  # plugins:
  #   - name: jwt
  #     service: httpbin_service
  #     enabled: true
  #     config:
  #       key_claim_name: iss
  #       claims_to_verify:
  #       - exp
  # consumers:
  # - username: liferay_server_issuer
  # jwt_secrets:
  #   - consumer: liferay_server_issuer
  #     secret: "e71829c351aa4242c2719cbfbe671c09"
  #     algorithm: HS256
  #     key: "http://my-issuer.com"
- name: geo_api_gouv_service
  host: geo.api.gouv.fr
  port: 443
  protocol: https
  path: /communes
  routes:
    - name: communes_route
      paths:
        - /cities
      strip_path: true