_format_version: "3.0"
_transform: true

services:
- name: example_service
  host: httpbin.org
  port: 80
  protocol: http
  routes:
    - name: example_route
      paths:
        - /mock
      strip_path: true